package com.example.jnpevent.model;

public enum RegistrationStatus {
    PENDING,   // Default status when a user registers
    APPROVED,  // Status when admin approves the registration
    REJECTED   // Status when admin rejects the registration
}
